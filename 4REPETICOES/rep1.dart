/*
Complete este trecho de código para exibir apenas o valor 25 no terminal.
Não é necessário inserir verificações condicionais no código. Só altere os
“???”, alterando-os por comandos adequados.
*/
void main() {
  int contador = 100;
  while (contador > 25) {
    contador--;
  }
  print("O valor de contador é $contador.");
}
